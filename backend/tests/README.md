# tests

Automated tests, including a golden set of claims with known verdicts so the
verifier's accuracy can be measured, not just eyeballed (from Milestone 1
onward, per [feedback on testing non-deterministic systems](../../docs/HLD.md)).
